import News from "../src/components/News/News";

describe("<News />", () => {
  it("renders the component correctly", () => {
    //const wrapper = mount(<News />);
    //console.log(wrapper.debug);
    //expect(wrapper.find('.card-title').toEqual(newsItem);
  });
});
